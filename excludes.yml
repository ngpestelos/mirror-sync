# Dest names that must not enter the daily matrix.
# Dest-only unique branch names survive a heads push (no prune).
# Exclude dests whose DEFAULT is dest-only work, or whose dest workflow is custom.

- yt-dlp          # dest-only play rebase; inventory custom dest workflow
